export type Challenge = {
  id: number;
  text: string;
  industry: string;
  role: string;
  difficulty: string;
  date: string;
};

export type CategoryOption = {
  value: string;
  label: string;
};